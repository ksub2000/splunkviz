#
# Splunk app configuration file
#

[install]
is_configured = $${configured}

[package]
id = splunkviz_radial_meter

[ui]
is_visible = $${visible}
label = $${label}

[launcher]
author = $${author}
description = $${description}
version = 1.0
